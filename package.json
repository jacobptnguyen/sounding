{
  "name": "sounding",
  "version": "0.1.0",
  "private": true,
  "description": "Describe a marine animal, get its photo and description from iNaturalist.",
  "scripts": {
    "dev": "node --env-file=.env.local dev-server.js",
    "test": "node test-identify.js"
  }
}
